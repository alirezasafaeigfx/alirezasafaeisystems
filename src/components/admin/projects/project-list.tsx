'use client'
import { Pencil, Trash2 } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import type { AdminProject } from './projects-manager'
export function ProjectList({ projects, onEdit, onDelete }: { projects: AdminProject[]; onEdit: (project: AdminProject) => void; onDelete: (project: AdminProject) => void }) { return <Card><CardHeader><CardTitle>Portfolio projects</CardTitle><CardDescription>{projects.length} project(s)</CardDescription></CardHeader><CardContent>{projects.length === 0 ? <div className="rounded-md border border-dashed p-6 text-center text-muted-foreground">No portfolio projects yet.</div> : <div className="space-y-3">{projects.map((project) => <article key={project.id} className="flex flex-col gap-3 rounded-lg border p-4 md:flex-row md:items-center md:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{project.title}</h2><Badge variant="outline">portfolio</Badge><Badge variant={project.published ? 'default' : 'secondary'}>{project.published ? 'Published' : 'Draft'}</Badge>{project.featured ? <Badge>Featured</Badge> : null}</div><p className="mt-1 truncate text-sm text-muted-foreground">#{project.order} · {project.description}</p></div><div className="flex shrink-0 gap-2"><Button type="button" variant="outline" size="sm" onClick={() => onEdit(project)} aria-label={`Edit ${project.title}`}><Pencil aria-hidden="true" className="me-2 h-4 w-4" />Edit</Button><Button type="button" variant="ghost" size="sm" onClick={() => onDelete(project)} aria-label={`Delete ${project.title}`}><Trash2 aria-hidden="true" className="h-4 w-4 text-destructive" /></Button></div></article>)}</div>}</CardContent></Card> }
