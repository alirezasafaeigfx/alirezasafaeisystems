# Codex V3.2 YOLO + LOOP Prompt

```text
YOLO + LOOP MODE. Execute heavy tasks end-to-end. Repo: alirezasafaeigfx/alirezasafaeisystems; GitHub main is source of truth.

Read AGENTS.md, FOCUS_POLICY, the canonical roadmap, ledger, checkpoint, V3.2 specs and approval/live-verification policies. The roadmap is the only task selector. R0-01, R0-02, R0-03A and R0-04 are REUSED-DONE; never dispatch or reverify them on unchanged inputs.

Start at R0-05A. Amend existing draft PR #21 only: make the ancestry/scope guard trigger from sensitive changed paths rather than branch names, run on its own PR, verify intended base/ancestry plus declared task/scope, and fail closed on unexpected categories. Do not build a parallel guard. Then handle R0-05B only with explicit repository-admin authorization; otherwise record the exact blocker. Review/accept existing draft PR #20 for R0-03B—do not reimplement it. Trigger R0-03C only with literal APPROVE_PHASE_2_STAGING_DEPLOY and require internal health, bounded smoke, two public browser passes, exact release identity and rollback evidence. Then observe R0-05 production truth and create R0-06 from the accepted base.

Then execute dependency-ready canonical tasks through S1→S5. Reuse existing portrait/components/CTA/analytics/URL-backed Discover/evidence before extending them. Gate A DOM/SVG/CSS/WAAPI is the only active immersive direction; Gate B/C are outside the queue and require new roadmap admission. Never publish `−58%` or `0/30d`; any metric, including `180→55` or `0 rollback / 21d`, requires typed provenance acceptance.

Use isolated worktrees and parallel agents only for independent, non-overlapping paths; ORCH alone integrates and writes the ledger. Before each claim, inspect code, tests, history, PRs/issues and exact-SHA evidence to prove the work is missing. Use TDD and root-cause fixes; preserve FA/EN, RTL/LTR, a11y, SEO, security, performance, reduced motion, authored mobile composition and truthful provenance. Reject fake PASS/metrics/proof, unrelated refactors, dependency churn, broad rewrites and repeated green checks.

Ask no routine questions: choose the safest high-value assumption, record it, continue. After each unit: inspect diff, verify fresh, commit atomically, update the ledger, select the next ready task, loop.

Stop only for a missing exact gate, security/data-loss risk, unavoidable external-access blocker, or zero safe valuable work. Never force-push, expose secrets, publish, migrate, merge, deploy, rollback or mutate production outside valid gates. Done means every roadmap DoD, independent review, full CI/visual evidence, governed staging, two live passes, exact release identity and rollback evidence.
```
